// Copy this file to config.js and fill in your values
// config.js is gitignored and will not be committed
export const CONFIG = {
  CLICKUP_API_TOKEN: '', // Get from: ClickUp → Settings → Apps → API Token
  CLICKUP_LIST_ID: '',   // The ID after /li/ in your ClickUp list URL
  CLICKUP_LIST_URL: '',  // Full URL to your ClickUp list (for the UI link)

  SUPABASE_URL: '',      // Your Supabase project URL
  SUPABASE_ANON_KEY: '', // Supabase anon/public key (Settings → API)
};
